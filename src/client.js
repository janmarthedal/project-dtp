import React from 'react';
import ReactDOM from 'react-dom';
import EditItemForm from './edit-item-form';

var container = document.getElementById('edit-item-form'),
    element = <EditItemForm />;

ReactDOM.render(element, container);

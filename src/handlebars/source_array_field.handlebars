<div class="control-group">
  <label class="control-label" for="{{id}}0">{{name}}</label>
  <div class="controls">
{{#each values}}
    <input type="text" class="{{../classes}}" id="{{../id}}{{@index}}" value="{{this}}" maxlength="{{../max}}"><br>
{{/each}}
  </div>
</div>
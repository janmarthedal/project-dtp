<div class="control-group">
  <label class="control-label" for="{{id}}">{{name}}</label>
  <div class="controls">
    <input type="text" class="{{classes}}" id="{{id}}" value="{{value}}" maxlength="{{max}}">
  </div>
</div>
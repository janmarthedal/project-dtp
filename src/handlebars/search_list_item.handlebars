<td>
<div><a href="{{item_link}}">{{item_name}}{{#if pritags}} ({{pritags}}){{/if}}</a></div>
<div><small class="pull-right">by <a href="{{author_link}}">{{author_name}}</a> at {{timestamp}}</small>
{{#each sectags}}<span class="tag">{{this}}</span>{{/each}}</div>
</td>